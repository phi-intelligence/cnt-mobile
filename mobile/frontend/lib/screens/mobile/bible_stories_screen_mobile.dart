import 'package:flutter/material.dart';

class BibleStoriesScreenMobile extends StatelessWidget {
  const BibleStoriesScreenMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bible Stories'),
      ),
      body: const Center(
        child: Text('Bible Stories Screen - Mobile - Coming Soon'),
      ),
    );
  }
}

